import { css } from "lit";

export const accountFormsCss = css`
    .center-content {
        padding: 1rem;
    }

    h1 {
        margin-bottom: 4rem;
        text-align: center;
    }

    button[type="submit"] {
        display: flex;
        font-size: var(--font-size-small);
        letter-spacing: 0.1rem;
        margin: 2rem auto;
        padding: 10px 44px;
        text-transform: uppercase;
    }
`;
